[project]
name = "binance-dist"
version = "0.1.0"
description = "Production-ready Binance Spot portfolio rebalancer CLI"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.12.4",
    "pytest>=8.4.2",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "tomli>=2.3.0",
]
